# Orchestration Log — Sokka (Tester)

**Timestamp:** 2026-03-05T16:44:00Z  
**Agent:** Sokka (QA/Tester)  
**Task:** Fix E2E test scenarios for timing/assertion issues  
**Mode:** Background  

## Outcome: SUCCESS

### Deliverables

1. **Test Infrastructure Improvements** (`tests/agents/src/scenario.ts`)
   - Added `waitFor<T>(fn, predicate, maxAttempts, delayMs)` polling helper
   - Retries async checks with configurable delay (default 1s, max 5 attempts)
   - Used for all notification assertions depending on backend-side async processing

2. **Test Client Fixes** (`tests/agents/src/client.ts`)
   - `acceptFriendship` / `declineFriendship`: Now send `{ action: "accept" }` / `{ action: "decline" }` (was `{ status: "accepted" }`)
   - `createMeetup`: Send camelCase keys (`friendIds`, `startTime`, `endTime`)
   - `createGroup`: Send `memberIds` (camelCase)
   - `getFriends()`: Map `friendshipId` → `id` for response normalization

3. **Test Scenario Fixes** (6 scenarios)
   - **friends.ts**: Added polling for notifications; scoped duplicate check to per-sender (Spontaneous vs. Planner)
   - **meetups.ts**: Added polling for `meetup_request` and `meetup_confirmed` notifications
   - **notification-dedup.ts**: Use `friend.id` instead of `user_a_id/user_b_id`; added polling
   - **calendar-events.ts**: Added required `id` field to test event; added polling for saved events list

4. **Test Results**
   - **Before:** 53 passing / 16 failing tests (77%)
   - **After:** 75 passing / 5 failing tests (94%)
   - **Failures are backend issues, not test bugs**:
     - 3 failures: `manual_busy_blocks` table missing (needs migration)
     - 1 failure: Availability seed (depends on table)
     - 1 failure: Group delete authorization gap

## Decision Created

**Decision: E2E Test Infrastructure Fixes**
- Location: `.squad/decisions/inbox/sokka-e2e-test-fixes.md`
- Status: Merged to `decisions.md`

## Cross-Agent Impact

- **Zuko (Backend Dev):** Backend fixes enable all 6 scenarios to pass once migration is applied
- **Toph (Schema):** Migration application will unblock 4 remaining failures
- **Katara (Frontend):** No changes needed
