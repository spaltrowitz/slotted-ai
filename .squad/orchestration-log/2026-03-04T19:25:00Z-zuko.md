# Orchestration Log — Zuko — 2026-03-04T19:25:00Z

**Agent:** Zuko (Backend Dev)  
**Session:** notification-dedup-review  
**Status:** Complete

## Work Completed

- **Review:** Notification deduplication fix in `functions/src/index.ts`
- **Issue found:** Migration's unique index on `(user_id, type, related_user_id)` covered both `friend_accepted` AND `friend_request`, but `friend_accepted` is overloaded (used for group membership changes). Index would silently block legitimate group notifications via 23505 constraint handler.
- **Fix applied:** Narrowed unique index to `friend_request` only. Scoped cleanup DELETE to match only friend acceptance patterns by title.
- **Rationale:** `friend_accepted` type serves dual purpose; DB constraint unsafe. App-level dedup (1-hour relatedUserId window) handles it. Future tech debt: split type into `group_update`.
- **Build status:** ✅ Passes

## Decision Output

- Merged to `.squad/decisions.md` as "Narrow Notification Dedup Index to friend_request Only"
- Files affected: `migrations/deduplicate_notifications.sql`
