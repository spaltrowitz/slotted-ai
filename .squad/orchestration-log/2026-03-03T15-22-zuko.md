# Zuko — Orchestration Log (2026-03-03T15:22)

## Task
Fix 3 critical calendar sync bugs from Sokka's code review.

## Outcome
✅ All 3 fixed. Build passes. Committed as 5db77f9.

### Fixes Implemented

#### CRIT-1: Feedback Loop Prevention (`rsvp_source` check)
- **File:** `functions/src/index.ts` ~7607–7640
- **Change:** Added `isRecentAppChange` guard to skip RSVP overwrites if `rsvp_source === 'app'` within 60s
- **Secondary Fix:** Cancelled-event path now explicitly updates etag before skipping

#### CRIT-2: Disconnect Cleanup
- **File:** `functions/src/index.ts` ~6536–6555
- **Change:** Added `calendar_watch_channel: null, calendar_watch_resource_id: null, calendar_sync_token: null` to user update
- **Secondary Change:** Separate query nulls `google_event_id` on all participant rows

#### CRIT-3: Webhook Returns 200 for All Requests
- **File:** `functions/src/index.ts` ~7800
- **Change:** Replaced `res.status(403)` with `console.warn()` + `res.status(200)`
- **Rationale:** Google deactivates endpoints that return 4xx errors

### Verification
- `cd functions && npm run build` ✅
- No schema changes required
- Ready for deployment

## Status
**APPROVED** — Ship ready pending user testing.
